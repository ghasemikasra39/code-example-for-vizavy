import React from 'react';

describe('ChatRoom Pusher subscription', () => {
  test('logged-in user joins a room from rooms tab', () => {

  });
  test('logged-in user joins a room via pressing on Push Notification', () => {

  });
  test('logged-in user joins a room via dynamic link', () => {

  });
  test('logged-in user creates a new room', () => {

  });
  test('logged-in user locks the phone', () => {

  });
  test('logged-in user unlocks the phone', () => {

  });
  test('logged-in user opens the app from killed state', () => {

  });
  test('logged-in user enters a room of which he was a member of', () => {

  });
});
